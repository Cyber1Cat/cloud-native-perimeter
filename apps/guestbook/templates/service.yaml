apiVersion: v1
kind: Service
metadata:
  name: guestbook-service
  namespace: default
spec:
  ports:
  - port: 443
    targetPort: 80 # Maps the secure perimeter down to the internal container port
    protocol: TCP
  selector:
    app: guestbook-ui
